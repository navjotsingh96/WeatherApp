import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.weather.app',
  appName: 'weather-app',
  webDir: 'dist/weather-app',
  bundledWebRuntime: false
};

export default config;
